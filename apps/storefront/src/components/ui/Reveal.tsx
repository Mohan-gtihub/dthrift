/**
 * Re-export kept for backwards compatibility. The implementation now lives
 * in `@/components/motion/Reveal` (Motion-based). Existing imports from
 * `@/components/ui/Reveal` continue to work unchanged.
 */
export { Reveal } from "@/components/motion/Reveal";
